import { types } from "./types";
import { quries } from "./queries";
import { resolvers } from "./reslovers";
import { mutations } from "./mutation";

export const User = { types, quries, resolvers, mutations };
